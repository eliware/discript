# Requirements

The numbered documents in this directory define Discript's language, runtime,
CLI, safety, output, and integration requirements. Implementations must
preserve those contracts and pass repository validation.
