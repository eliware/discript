// Normalize a failed operation into the same shape an agent receives remotely.
result = try {
  discord.guilds.get(env.DISCRIPT_UNKNOWN_GUILD)
} catch (error) {
  {ok: false, requestId: env.DISCRIPT_REQUEST_ID, code: error.code, exitCode: error.exitCode, error: error.message}
}
result

