// Turn a message event into a small machine-readable triage record.
on("messageCreate") {
  if (event.author.bot != true) {
    urgent = event.content != null && event.content != "" && event.content
    print({
      kind: "triage",
      messageId: event.id,
      channelId: event.channelId,
      authorId: event.author.id,
      urgent: urgent
    })
  }
}
// Mini tutorial: message triage.
// This example demonstrates composition, decision-making, and resilient workflows.
// Run it with: discript examples/clever/message-triage.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
