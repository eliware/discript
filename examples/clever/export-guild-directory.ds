// Emit a directory-shaped object suitable for another program to consume.
guildId = env.TEST_GUILD
if (guildId == null) {
  exit(2, "set TEST_GUILD")
}

guild = discord.guilds.get(guildId)
directory = {
  guild: {id: guild.id, name: guild.name},
  channels: guild.channels.list(),
  roles: guild.roles.list(),
  events: guild.scheduledEvents.list(),
  invites: guild.invites.list()
}
print(directory)
// Mini tutorial: export guild directory.
// This example demonstrates composition, decision-making, and resilient workflows.
// Run it with: discript examples/clever/export-guild-directory.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
