guildId = "<guild-id>"
userId = "<user-id>"

preview = discord.guilds.get(guildId).members.get(userId).timeout(3600000, "review required", {dryRun: true})
print(preview)
// Mini tutorial: moderation preview.
// This example demonstrates Discord API operations with safety controls.
// Run it with: discript examples/discord/moderation-preview.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
/* Tutorial focus: This demonstrates a safe moderation plan: validate a member and return a dry-run decision. */
