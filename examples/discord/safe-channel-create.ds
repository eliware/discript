guildId = "<guild-id>"
channelName = "agent-created-channel"

preview = discord.guilds.get(guildId).channels.create(channelName, {dryRun: true})
print(preview)

result = try {
  discord.guilds.get(guildId).channels.create(channelName, {force: true})
} catch (error) {
  print({error: error.message, code: error.code, exitCode: error.exitCode})
  error
}
result
// Mini tutorial: safe channel create.
// This example demonstrates Discord API operations with safety controls.
// Run it with: discript examples/discord/safe-channel-create.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
