// Safe preview: no Discord mutation is made.
guildId = "<guild-id>"
preview = discord.guilds.get(guildId).channels.create("agent-preview", {dryRun: true})
print(preview)
// Mini tutorial: dry run channel.
// This example demonstrates Discord API operations with safety controls.
// Run it with: discript examples/discord/dry-run-channel.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
