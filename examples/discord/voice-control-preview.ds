guildId = "<guild-id>"
userId = "<user-id>"
channelId = "<voice-channel-id>"

preview = discord.guilds.get(guildId).members.get(userId).voice.move(channelId, {dryRun: true})
preview
// Mini tutorial: voice control preview.
// This example demonstrates Discord API operations with safety controls.
// Run it with: discript examples/discord/voice-control-preview.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
