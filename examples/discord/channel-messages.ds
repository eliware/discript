channelId = "<channel-id>"
messageId = "<message-id>"
message = discord.messages.get(channelId, messageId)
print({id: message.id, author: message.author, content: message.content})
// Mini tutorial: channel messages.
// This example demonstrates Discord API operations with safety controls.
// Run it with: discript examples/discord/channel-messages.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
