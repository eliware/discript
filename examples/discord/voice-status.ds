guildId = "<guild-id>"
voice = discord.voice.status(guildId)
print(voice)

memberId = "<user-id>"
memberVoice = discord.guilds.get(guildId).members.get(memberId).voice.status()
memberVoice
// Mini tutorial: voice status.
// This example demonstrates Discord API operations with safety controls.
// Run it with: discript examples/discord/voice-status.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
