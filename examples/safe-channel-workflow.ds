guildId = "<guild-id>"
channelName = "agent-preview"

preview = discord.guilds.get(guildId).channels.create(channelName, {dryRun: true})
print(preview)

result = try {
  discord.guilds.get(guildId).channels.create(channelName, {force: true})
} catch (error) {
  print(error)
  error
}

if (result.ok == false) {
  exit(result.exitCode, "channel creation failed")
}

result
// Mini tutorial: safe channel workflow.
// This example demonstrates Discript scripting fundamentals.
// Run it with: discript examples/safe-channel-workflow.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
/* Tutorial focus: This demonstrates a reusable safe mutation workflow with preview and explicit approval paths. */
