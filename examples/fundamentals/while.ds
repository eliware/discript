count = 0
while (count < 3) {
  print({iteration: count})
  count = count + 1
}
count
// Mini tutorial: while.
// This example demonstrates core Discript syntax and runtime behavior.
// Run it with: discript examples/fundamentals/while.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
