total = 0
for (value in [1, 2, 3, 4]) {
  total = total + value
}

labels = {first: "one", second: "two"}
for (label in labels) {
  print(label)
}

total
// Mini tutorial: loops.
// This example demonstrates core Discript syntax and runtime behavior.
// Run it with: discript examples/fundamentals/loops.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
/* Tutorial focus: This demonstrates bounded iteration over collections and accumulating a result. */
