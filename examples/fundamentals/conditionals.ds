score = 87

if (score >= 90) {
  grade = "A"
} else if (score >= 80) {
  grade = "B"
} else {
  grade = "needs review"
}

{score: score, grade: grade}
// Mini tutorial: conditionals.
// This example demonstrates core Discript syntax and runtime behavior.
// Run it with: discript examples/fundamentals/conditionals.ds (or pipe it to discript for stdin mode).
// Read the final value and comments together to follow the data flow.
