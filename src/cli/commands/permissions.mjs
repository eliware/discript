import { required } from './validation.mjs';
export function createPermissionsHandler({command,options,api}) { if(command[0]!=='permissions'||!['list','set','delete'].includes(command[1])) return {handled:false}; const op=command[1],p=api.channels.get(required(options,'channel',`permissions ${op} requires --channel <id>.`,'CHANNEL_REQUIRED')).permissions; if(op==='list') return {handled:true,value:p.list()}; const t=required(options,'target',`permissions ${op} requires --target <id>.`,'TARGET_REQUIRED'); if(op==='delete') return {handled:true,value:p.delete(t,{reason:options.reason})}; if(options.allow===undefined&&options.deny===undefined) throw Object.assign(new Error('permissions set requires --allow and/or --deny.'),{code:'PERMISSIONS_REQUIRED',exitCode:2}); return {handled:true,value:p.set(t,{...(options.allow!==undefined?{allow:options.allow.split(',').filter(Boolean)}:{}),...(options.deny!==undefined?{deny:options.deny.split(',').filter(Boolean)}:{})},{reason:options.reason})}; }
