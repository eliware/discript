import { required } from './validation.mjs';
export function createEventsHandler({command,options,api}) { if(command[0]!=='events'||!['list','create','update','delete'].includes(command[1])) return {handled:false}; const op=command[1],g=api.guilds.get(required(options,'guild',`events ${op} requires --guild <id>.`,'GUILD_REQUIRED')).scheduledEvents; if(op==='list') return {handled:true,value:g.list()}; if(op==='create') return {handled:true,value:g.create({name:required(options,'name','events create requires --name <name>.','NAME_REQUIRED'),scheduledStartTime:required(options,'start','events create requires --start <ISO-8601 time>.','START_REQUIRED'),description:options.description,entityType:options.event_type?Number(options.event_type):3,entityMetadata:options.location?{location:options.location}:undefined})}; const e=g.get(required(options,'event',`events ${op} requires --event <id>.`,'EVENT_REQUIRED')); if(op==='delete') return {handled:true,value:e.delete()}; if(!options.name&&!options.description&&!options.start) throw Object.assign(new Error('events update requires --name, --description, or --start.'),{code:'EVENT_FIELDS_REQUIRED',exitCode:2}); return {handled:true,value:e.update({...options.name?{name:options.name}:{},...options.description?{description:options.description}:{},...options.start?{scheduledStartTime:options.start}:{}})}; }
