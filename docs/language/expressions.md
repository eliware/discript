# Expressions

Expressions include literals, identifiers, arrays, objects, function calls, member access, operators, arrows, awaits, and try expressions. Expression results can be assigned, printed, returned, or passed to the next Discord operation.

