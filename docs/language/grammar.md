# Grammar

The parser implementation in `src/parser/` is the executable grammar authority. This page intentionally gives the conceptual surface; update it alongside tokenizer or parser changes rather than maintaining a contradictory handwritten grammar.

