# Statements

The language supports imports, functions, returns, assignments, event handlers, timers, conditionals, and loops. Blocks are required for control-flow bodies, which keeps scripts visually explicit.

