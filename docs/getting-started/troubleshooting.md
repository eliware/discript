# Troubleshooting

Check `DISCORD_TOKEN`, Node.js version, bot guild visibility, and Discord permissions first. Use `--json` to inspect stable error codes. For live tests, verify `TEST_GUILD` points to the disposable test server and that the bot is present.

