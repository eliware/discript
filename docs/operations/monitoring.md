# Monitoring

Use a read-only health script to verify authentication, guild visibility, and channel access. Monitor process exit status, structured error codes, connection lifetime, and rate-limit/API failures. Do not treat a connected client as proof that every operation is authorized.

