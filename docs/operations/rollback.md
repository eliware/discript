# Rollback

Package rollback means installing a previously published version. Discord-state rollback means running a compensating script with recorded IDs; Discord does not provide a universal transaction. Preview cleanup and require explicit approval for deletion.

