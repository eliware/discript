# CI

CI should run unit tests, lint, package verification, and a dry-run package check. Live Discord tests are opt-in and must use isolated credentials and the disposable test guild. Keep notification-heavy full CI runs intentional.

