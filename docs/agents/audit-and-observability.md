# Audit and observability

Emit operation names, target IDs, mode (`preview` or `applied`), and final status. Redact tokens, webhook secrets, and sensitive message content before forwarding output to an agent transcript.

