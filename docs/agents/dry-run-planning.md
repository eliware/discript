# Dry-run planning

Dry-run output should be treated as a plan, not proof that the later live operation will succeed. Permissions, races, rate limits, and state can change between phases; revalidate before applying.

