# Exit-code branching

Use distinct script statuses for outcomes such as success, validation failure, approval required, rollback completed, and retryable Discord failure. Keep the mapping documented so the harness does not guess.

