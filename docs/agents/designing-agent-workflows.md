# Designing agent workflows

Make each workflow observable: inventory, plan, validate, approve, apply, verify. Return a compact result with target IDs, changed IDs, skipped items, and a suggested next action.

