# Security boundaries

The process has the authority of its bot token. A generated script is not sandboxed from Discord state, the filesystem, or environment access; review it before granting approval and isolate credentials for automation.

