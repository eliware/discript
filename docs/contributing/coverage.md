# Coverage

Coverage is evidence, not the only quality measure. The project’s strongest gate is four-column coverage for focused modules where required; also verify behavior, error paths, safety classification, and live capability boundaries.

