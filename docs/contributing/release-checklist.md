# Release checklist

- Update version and release notes.
- Run tests, lint, package verification, audit, and pack checks.
- Confirm docs/examples/specs describe user-visible changes.
- Tag the release and run the standard publish workflow.
- Verify installation and CLI behavior from the published package.

