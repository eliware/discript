# Coding style

Follow the existing ESM, Node.js, and repository conventions. Prefer small single-purpose modules, explicit errors with stable codes, normalized return values, and tests that mirror implementation files.

