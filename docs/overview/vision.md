# Vision

Discript makes Discord automation composable, inspectable, and safe for humans and AI agents. A single command and a multi-step `.ds` program use the same resource model, output contracts, and approval rules.

Scripts can be piped without temporary files, reused as deployment templates, or exposed through one MCP tool with discoverable help and contracts. Connection reuse, deterministic output, and explicit lifecycle rules make repeated agent calls reliable.
