# Architecture overview

The CLI selects direct-command or script execution. Both paths use the runtime for configuration, Discord connection, API access, output, exit status, and shutdown. Parser and adapter boundaries are documented in the developer guide.

