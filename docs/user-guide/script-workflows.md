# Script workflows

Scripts combine values returned by one operation with later calls, functions, loops, conditionals, imports, and event handlers. Keep read/plan/apply phases visible and return a final object useful to a human or agent.

