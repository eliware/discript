# Mutations

Use `--dry-run --validate` to inspect a direct command before `--yes`. In scripts, pass `{dryRun: true}` or `{force: true}` to the operation. Prefer stable lookup criteria and report every created, updated, and deleted ID.

