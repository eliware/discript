# Read-only inventory

Start with `examples/agent/backup-guild.ds` or `examples/agent/read-only-inventory.ds`. Inventory scripts should not pass `force`, should avoid credentials in output, and should emit IDs as strings.

