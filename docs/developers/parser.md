# Parser

Parser responsibilities are split into tokenizer, token stream, statements, expressions, operators, errors, and the public index entry point. Add syntax tests beside the affected module and preserve useful parse error locations/codes.

