# CLI commands

Adding a command normally touches the catalog, handler, option validation/mutation classification, help/completion output, tests, and user-facing documentation. Keep handlers focused and delegate Discord work to adapters.

