# Development setup

Clone the repository, run `npm install`, copy `.env.example` to `.env`, and keep live credentials local. Use `npm test`, `npm run lint`, and `npm run package:check` before proposing changes.

