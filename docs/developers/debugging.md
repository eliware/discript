# Debugging

Reproduce with `--json`, isolate parser failures by running `parse()` on the source, and inspect Discord failures by checking the target ID, bot permissions, and returned error code. Keep tokens and webhook secrets out of diagnostics.

