# Adding a language feature

Define the syntax and runtime semantics in the relevant spec, update tokenizer/parser modules, add 1:1 parser and evaluator tests, add a small example, then run the full suite. Document ambiguity and error behavior before merging.

