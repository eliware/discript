# Test guild

`TEST_GUILD` identifies the disposable live-test server. Mutation examples and live tests must scope every lookup to it, use unique names where needed, and clean up resources. Never copy its token or assume IDs are portable.

