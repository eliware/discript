# Commands

The live command catalog is available with `discript commands list --json`. Resource families include guilds, channels, roles, members, messages, threads, permissions, webhooks, invites, events, emojis, stickers, voice, and moderation. This generated/discoverable catalog is authoritative as commands evolve.

