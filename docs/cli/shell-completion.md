# Shell completion

Generate completion scripts with `discript completion bash`, `discript completion zsh`, or `discript completion fish`. Review generated shell code before installing it into a shared profile.

