# Standard input

When no script file is supplied and source is available on stdin, Discript evaluates that source. This supports ephemeral agent plans and avoids temporary files. Use explicit `--json` when another process consumes the result.

