# JSON output

`--json` returns structured command results and structured errors. `--output jsonl` is appropriate when scripts emit multiple values. Treat IDs as strings and inspect `exitCode` in errors.

