# Resource model

Guild-scoped resources are reached through `discord.guilds.get(id)`. Channel-scoped resources are reached through `discord.channels.get(id)`. Returned values are normalized plain data suitable for JSON and subsequent script expressions.

